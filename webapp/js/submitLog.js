function log(){
	
	var eggs=document.forms["proteinForm"]["eggs"].value;
	var eggNumber = document.getElementById('Results');
	var test=6;
	eggNumber.innerHTML = test;
	/*var ul = document.getElementById('Makes');
	for (var j = 0; j < data.length; j += 2) {
	var li = document.createElement('li');
	li.setAttribute("class", "list-group-item");
	if (j + 1 < data.length - 1)
	li.setAttribute("style","border-bottom-width: 0px");
	li.innerHTML = data[j];
	ul.appendChild(li);
	$('#select').empty();
	var newSelect = document.getElementById('select');
	for (var i = 0; i < j + 1; i += 2) {
	var opt = document.createElement("option");
	opt.value = data[i];
	newSelect.appendChild(opt);
	opt.innerHTML = data[i];*/
	
	
}